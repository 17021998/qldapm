package com.example.hearthrate;

public class test {
/*

        Double value = calc.exec(brightness, this.getSeconds(), this.fps);
        if(this.brightness.size()%17==0 & value >= 60)
            save_avg = value;
 */
}
